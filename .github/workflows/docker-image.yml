name: Docker Image CI

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Docker Login
      env:
        DOCKER_USER: ${{secrets.DOCKER_USER}}
        DOCKER_PASSWORD: ${{secrets.DOCKER_PASSWORD}}
      run: |
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
    - name: Build the Docker image (latest)
      run: docker build . --file Dockerfile --tag ${{secrets.DOCKER_USER}}/kalieduxo:latest
    - name: Build the Docker image (${{GITHUB.REF.NAME}})
      run: docker build . --file Dockerfile --tag ${{secrets.DOCKER_USER}}/kalieduxo:${{GITHUB.REF.NAME}}
    - name: Docker Push (latest)
      run: docker push ${{secrets.DOCKER_USER}}/kalieduxo:latest
    - name: Docker Push (${{GITHUB.REF.NAME}})
      run: docker build . --file Dockerfile --tag ${{secrets.DOCKER_USER}}/kalieduxo:latest
